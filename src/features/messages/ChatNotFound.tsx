import { Message } from '.';

export const ChatNotFound = () => {
	return (
		<>
			{/* <Message
				id="xxx"
				avatarUrl=""
				body="Do you think God stays in heaven because he too lives in fear of what he's created."
				isLoading={true}
			/> */}
			<h2 className="text-3xl">Select a valid channel to start messaging.</h2>
		</>
	);
};
